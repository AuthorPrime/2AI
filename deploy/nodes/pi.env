# Pi 5 "The Foundation" — Always-On Persistent Layer
NODE_ID=pi
NODE_ROLE=infrastructure
HOSTNAME=redis-pi

# Redis (local primary)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Node services
NODE_SERVICES=redis,lattice-health
